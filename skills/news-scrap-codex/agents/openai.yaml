interface:
  display_name: "응급의료 뉴스 스크랩 Codex"
  short_description: "Codex 선정·리서치와 NotebookLM slide-deck 기반 주간 브리핑 생성"
  default_prompt: "Use $news-scrap-codex to create a Korean weekly emergency-medicine AI news briefing where Codex searches, verifies, summarizes, selects one featured article, researches it, and NotebookLM generates only the final slide deck."

policy:
  allow_implicit_invocation: true
